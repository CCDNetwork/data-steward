import { PageContainer } from '@/components/PageContainer';

export const DeduplicationPage = () => {
  return (
    <PageContainer pageTitle="Deduplication">
      <div>Deduplication Page</div>
    </PageContainer>
  );
};
